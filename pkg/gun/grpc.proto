syntax = "proto3";
import "google/protobuf/empty.proto";
option go_package = "../proto";

message Hunk {
  bytes Data = 1;
}

service Gun {
  rpc OK(google.protobuf.Empty) returns (google.protobuf.Empty);
  rpc Tun (stream Hunk) returns (stream Hunk);
}